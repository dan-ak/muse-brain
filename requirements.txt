# Full development stack: the Qt visualizer, the OSC path, and the PWA host.
# The Pi only needs requirements-server.txt.
-r requirements-server.txt

python-osc>=1.8
pyqtgraph>=0.13
numpy>=1.26
scipy>=1.11
PyQt5>=5.15
trimesh>=4.0
Pillow>=10.0
pytest>=8.0

# Reading and summarising recorded sessions (analysis/).
pandas>=2.0
matplotlib>=3.7

